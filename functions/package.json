{
  "name": "iknowthisone-functions",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/tasks": "^5.5.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5",
    "resend": "^6.12.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
